{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "zeroclaw.apiKeySecretName" . }}
  labels:
    {{- include "zeroclaw.labels" . | nindent 4 }}
type: Opaque
stringData:
  API_KEY: {{ .Values.secret.apiKey | quote }}
{{- end }}
